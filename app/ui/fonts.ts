import { Figtree, Cormorant_Garamond } from 'next/font/google';

export const nameBigFont = Figtree({ subsets: ['latin'] });
export const serif = Cormorant_Garamond({ subsets: ['latin'] });
export const sans = Figtree({ subsets: ['latin'] });